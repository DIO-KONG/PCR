# docs/ CLAUDE

- 文档描述模块职责、算法笔记、当前任务和结果解释。
- 当前 prompt 或实验计划写入 `current_task.md`。
- 不在文档目录存放运行结果、缓存或原始点云。
- 根目录 CLAUDE.md 记录长期工程约束；docs/current_task.md 记录当前 prompt 和阶段性计划。
- 算法调试记录写入 algorithm_notes.md，结果字段解释写入 result_interpretation.md。
- 当前主方案 `fast_rotation_cluster_scale_vote` 的设计、默认参数、验收结论和适用边界应同步记录在 `algorithm_notes.md`。
