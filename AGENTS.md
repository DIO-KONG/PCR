# AGENTS.md

* 环境：WSL Ubuntu 22.04；Python 环境为 `.env`。允许在 `.env` 中安装依赖。
* 第三方源码、构建产物和可执行文件必须放在 `dependencies/`，禁止安装到系统目录。

## 禁止事项

* 禁止把算法、状态更新、评估和文件输出重新堆进同一个大函数。
* 禁止在 `testbench/` 或 CLI 中实现核心算法；核心实现只能存在于 `pcr/`。
* 禁止用大型嵌套 `dict` 表达稳定的跨模块数据。
* 禁止使用语义不明的变换矩阵；统一遵守：
  `p_target = T_source_to_target @ p_source`
* 禁止声称测试通过，除非实际执行过对应命令。

## 修改要求

* 修改前先阅读相关实现、调用方、配置和测试。
* 架构修改必须保持功能可运行，并通过最小实际 smoke test。
* 新算法必须先说明适用条件、预期解决的问题、计算代价和失败模式，再实现。
* 文档只在行为、接口、架构或算法发生变化时更新；不要为无关改动批量修改 README。
* 注释解释不变量、坐标系、数值约束和设计原因，不要复述代码。

## 完成汇报

只汇报：

* 改了什么；
* 为什么改；
* 实际运行了哪些验证；
* 关键结果；
* 尚未验证或存在的风险。
